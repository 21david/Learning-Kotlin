package udemy.kotlincourse.section04

// Video 28

fun main(args: Array<String>) {
    // Any is like the Object class in Java: it's the root of every Kotlin object,
    // it has methods for toString(), hashCode(), and equals()

    val anObject: Any

    // The Unit class is equivalent to void in Java. (like in Scala)
    // In Kotlin, if the return type is Unit, then it really means it's not meant to return anything

    // Nothing is a subclass of every class
}